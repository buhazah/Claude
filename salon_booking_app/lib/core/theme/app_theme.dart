/// Legacy path — the canonical theme moved into the design system.
/// Existing imports keep working; new code should import
/// `core/design_system/design_system.dart`.
library;

export '../design_system/theme/app_theme.dart';
