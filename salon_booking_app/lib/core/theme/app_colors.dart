/// Legacy path — the canonical color tokens moved into the design system.
/// Existing imports keep working; new code should import
/// `core/design_system/design_system.dart`.
library;

export '../design_system/tokens/app_colors.dart';
