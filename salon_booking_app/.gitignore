# Flutter / Dart
.dart_tool/
.packages
build/
.flutter-plugins
.flutter-plugins-dependencies
.metadata

# IDE
.idea/
.vscode/
*.iml

# Platform folders are generated with `flutter create .` — commit them once generated.

# Firebase local config (generated by `flutterfire configure` — safe to commit,
# but keys per environment are usually managed outside VCS in real teams).
# lib/firebase_options.dart is committed here as a placeholder.
google-services.json
GoogleService-Info.plist

# Cloud Functions
firebase/functions/node_modules/
firebase/functions/lib/
*.log
