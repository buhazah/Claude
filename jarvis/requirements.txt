fastapi>=0.110
uvicorn[standard]>=0.27
pydantic[email]>=2.6
sqlalchemy>=2.0
aiosqlite>=0.19
httpx>=0.26
numpy>=1.26
python-multipart>=0.0.9
websockets>=12.0
cryptography>=42.0
# Document handling: read uploaded PDFs/Word files, and export real PDF/DOCX.
pypdf>=4.0
python-docx>=1.1
reportlab>=4.0
# Async Postgres driver — used automatically when JARVIS_DATABASE_URL points at
# Postgres (e.g. the one-click cloud deploys). Harmless when using SQLite.
asyncpg>=0.29
